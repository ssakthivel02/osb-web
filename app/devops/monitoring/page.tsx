import SiteHeader from '../../../components/site-header';

const signals=[['Metrics','Numeric trends for utilisation, saturation, errors, latency and business outcomes.'],['Logs','Timestamped events with structured context, correlation identifiers and searchable fields.'],['Traces','End-to-end request flow across services, queues, databases and external dependencies.'],['Events','Deployment, scaling, configuration and platform changes that explain behaviour.'],['SLOs','Explicit reliability targets derived from user-visible service expectations.'],['Alerts','Actionable notifications tied to impact, ownership, runbooks and escalation.']];

export default function MonitoringPage(){return <><SiteHeader/><main><section className="pageHero compactHero"><p className="eyebrow">DevOps module 07</p><h1>Observe user impact, not dashboard noise.</h1><p className="lead">Monitoring tells you that something is wrong. Observability helps you determine why.</p></section><section className="section"><div className="grid">{signals.map(([t,d],i)=><article className="card" key={t}><span className="eyebrow">{String(i+1).padStart(2,'0')}</span><h2>{t}</h2><p>{d}</p></article>)}</div></section><section className="section"><h2>Alert-quality test</h2><div className="moduleList"><article className="module"><span>01</span><h3>Is there customer impact?</h3><p>A technical threshold without impact may belong on a dashboard rather than a pager.</p></article><article className="module"><span>02</span><h3>Can someone act?</h3><p>Every alert needs ownership, context and a tested first-response procedure.</p></article><article className="module"><span>03</span><h3>Can we verify recovery?</h3><p>Define the signal that proves service health has returned.</p></article></div></section></main></>}