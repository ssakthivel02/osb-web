import SiteHeader from '../../../components/site-header';
const questions=[['How would you design a production CI/CD pipeline?','Start with source control, required checks, reproducible builds, tests, security scans, immutable artifacts, environment approvals, deployment strategy, telemetry and rollback.'],['What is the difference between continuous delivery and continuous deployment?','Continuous delivery keeps every validated change releasable; continuous deployment automatically releases every change that passes the controls.'],['How do you manage Terraform state safely?','Use a remote encrypted backend, locking, least privilege, environment separation, versioning, backup and controlled state operations.'],['How do you troubleshoot a failing Kubernetes deployment?','Inspect rollout status, events, pod state, logs, probes, resource limits, configuration, secrets, networking and dependencies before choosing rollback or repair.'],['What makes an alert actionable?','It identifies user impact, has a clear owner, avoids duplicate noise, includes context and links to a tested response procedure.'],['How do you secure a software supply chain?','Protect branches, pin trusted actions, scan dependencies and images, manage secrets externally, generate provenance and SBOMs, sign artifacts and enforce policy.']];
export default function Interview(){return <><SiteHeader/><main><section className="pageHero compactHero"><p className="eyebrow">Interview preparation</p><h1>Answer with architecture, evidence and trade-offs.</h1><p className="lead">Weak answers name tools. Strong answers explain controls, failure modes, operational evidence and why the design fits the risk.</p></section><section className="section"><div className="moduleList">{questions.map(([q,a],i)=><article className="module" key={q}><span>{String(i+1).padStart(2,'0')}</span><h3>{q}</h3><p>{a}</p></article>)}</div></section></main></>}
