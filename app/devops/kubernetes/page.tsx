import SiteHeader from '../../../components/site-header';

const objects=[['Pod','Smallest schedulable unit; normally managed rather than created directly.'],['Deployment','Maintains replicas and performs controlled rolling application updates.'],['Service','Provides stable discovery and traffic routing to changing pod instances.'],['Ingress','Routes external HTTP traffic through a managed ingress controller.'],['ConfigMap','Stores non-sensitive runtime configuration outside the container image.'],['Secret','Carries sensitive values but still requires encryption and access controls.'],['HPA','Scales replicas from measured demand and defined resource requests.'],['Namespace','Creates an organisational boundary—not a complete security boundary.']];

export default function KubernetesPage(){return <><SiteHeader/><main><section className="pageHero compactHero"><p className="eyebrow">DevOps module 05</p><h1>Operate workloads, not individual servers.</h1><p className="lead">Understand the control plane, desired state and workload primitives before memorising kubectl commands.</p></section><section className="section"><div className="grid">{objects.map(([t,d],i)=><article className="card" key={t}><span className="eyebrow">{String(i+1).padStart(2,'0')}</span><h2>{t}</h2><p>{d}</p></article>)}</div></section><section className="section"><h2>AKS production thinking</h2><div className="moduleList"><article className="module"><span>01</span><h3>Identity</h3><p>Prefer workload identity and managed identities over stored cloud credentials.</p></article><article className="module"><span>02</span><h3>Availability</h3><p>Spread replicas, use disruption budgets and test node maintenance behaviour.</p></article><article className="module"><span>03</span><h3>Observability</h3><p>Collect workload logs, cluster metrics, events and deployment evidence.</p></article></div></section></main></>}