import SiteHeader from '../../../components/site-header';

const topics=[['Image','Immutable application package built from ordered Dockerfile layers.'],['Container','Running process created from an image with isolated filesystem and networking.'],['Registry','Versioned image store used to distribute trusted artifacts.'],['Volume','Persistent data mounted outside the disposable container layer.'],['Network','Controlled communication path between containers and external services.'],['Security','Run as non-root, minimise packages, scan images and avoid embedded secrets.']];

export default function DockerPage(){return <><SiteHeader/><main><section className="pageHero compactHero"><p className="eyebrow">DevOps module 03</p><h1>Package once. Run consistently.</h1><p className="lead">Learn how Docker turns application source into a portable, inspectable and versioned runtime artifact.</p></section><section className="section"><div className="grid">{topics.map(([t,d],i)=><article className="card" key={t}><span className="eyebrow">{String(i+1).padStart(2,'0')}</span><h2>{t}</h2><p>{d}</p></article>)}</div></section><section className="section"><h2>Build checklist</h2><div className="moduleList"><article className="module"><span>01</span><h3>Small base image</h3><p>Use a supported minimal image and pin deliberate versions.</p></article><article className="module"><span>02</span><h3>Layer efficiency</h3><p>Copy dependency manifests before source to improve cache reuse.</p></article><article className="module"><span>03</span><h3>Runtime safety</h3><p>Use a non-root user, health checks and read-only configuration where practical.</p></article></div></section></main></>}