import SiteHeader from '../../../components/site-header';

const flow=[['Write','Declare providers, resources, data sources, variables, outputs and module boundaries.'],['Init','Download providers, initialise the backend and validate dependency locks.'],['Plan','Review the exact proposed change, replacements, deletions and unknown values.'],['Apply','Execute an approved plan with controlled credentials and auditable output.'],['State','Protect remote state with locking, encryption, access control and recovery.'],['Operate','Detect drift, upgrade providers carefully and refactor without recreating resources.']];

export default function TerraformPage(){return <><SiteHeader/><main><section className="pageHero compactHero"><p className="eyebrow">DevOps module 04</p><h1>Infrastructure changes need evidence.</h1><p className="lead">Terraform is valuable when state, review, policy and lifecycle discipline are treated as first-class controls.</p></section><section className="section"><div className="moduleList">{flow.map(([t,d],i)=><article className="module" key={t}><span>{String(i+1).padStart(2,'0')}</span><h3>{t}</h3><p>{d}</p></article>)}</div></section><section className="section"><h2>Production rules</h2><div className="grid"><article className="card"><h2>Remote state</h2><p>Never rely on an engineer's laptop as the production source of truth.</p></article><article className="card"><h2>Plan review</h2><p>Require peer review for destructive and replacement operations.</p></article><article className="card"><h2>Separate environments</h2><p>Keep clear state boundaries and credentials for development, test and production.</p></article><article className="card"><h2>No manual drift</h2><p>Import legitimate resources or revert unapproved console changes.</p></article></div></section></main></>}