import SiteHeader from '../../../components/site-header';

const method=[['Stabilise','Protect users first: stop harmful automation, freeze risky changes and restore a known-good path.'],['Scope','Identify affected users, regions, services, versions and the precise start time.'],['Correlate','Compare failures with deployments, configuration, infrastructure and dependency events.'],['Hypothesise','Rank likely causes from evidence; do not change multiple variables at once.'],['Test','Run the smallest safe experiment that can confirm or reject one hypothesis.'],['Recover','Rollback, roll forward or fail over with explicit validation criteria.'],['Learn','Capture timeline, contributing factors, control gaps and owned corrective actions.']];

export default function TroubleshootingPage(){return <><SiteHeader/><main><section className="pageHero compactHero"><p className="eyebrow">DevOps module 08</p><h1>Troubleshoot with evidence, not panic.</h1><p className="lead">A disciplined incident method reduces recovery time and prevents random changes from making the failure worse.</p></section><section className="section"><div className="moduleList">{method.map(([t,d],i)=><article className="module" key={t}><span>{String(i+1).padStart(2,'0')}</span><h3>{t}</h3><p>{d}</p></article>)}</div></section><section className="section"><h2>First five questions</h2><div className="grid"><article className="card"><h2>What changed?</h2><p>Deployment, secret, certificate, network, policy, dependency or infrastructure.</p></article><article className="card"><h2>Who is affected?</h2><p>All users or one path, tenant, region, browser, node or identity group.</p></article><article className="card"><h2>What still works?</h2><p>Healthy paths narrow the fault domain faster than staring at the failure alone.</p></article><article className="card"><h2>Can we roll back?</h2><p>Know the last verified release and the data compatibility implications.</p></article></div></section></main></>}