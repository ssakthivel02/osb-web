import SiteHeader from '../../../components/site-header';
const labs=[['Lab 01','Repository workflow','Create a feature branch, commit a change and explain the pull-request quality gates.'],['Lab 02','Pages pipeline','Trace the OSB GitHub Actions workflow from trigger to deployed artifact.'],['Lab 03','Docker delivery','Containerise a web application and document build, run and rollback commands.'],['Lab 04','Terraform plan','Model a small Azure environment and review the plan before applying changes.'],['Lab 05','AKS deployment','Deploy a workload, expose it safely and troubleshoot readiness failures.'],['Lab 06','Observability','Define service indicators, an SLO and actionable alerts for the application.'],['Lab 07','Secure pipeline','Add dependency, secret and image scanning gates without blocking all delivery.'],['Lab 08','Incident recovery','Diagnose a failed deployment, choose rollback or roll-forward and record evidence.']];
export default function Labs(){return <><SiteHeader/><main><section className="pageHero compactHero"><p className="eyebrow">Hands-on labs</p><h1>Practise the failure paths, not only the happy path.</h1><p className="lead">Each lab requires an implementation, validation evidence, troubleshooting notes and a rollback decision.</p></section><section className="section"><div className="grid">{labs.map(([lab,title,detail])=><article className="card" key={lab}><span className="eyebrow">{lab}</span><h2>{title}</h2><p>{detail}</p></article>)}</div></section></main></>}
